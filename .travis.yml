# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: FALSE
cache: packages

os:
- linux
- osx

r_binary_packages:
    - libgit2
    - libgit2-dev
    - libgit2-devel
